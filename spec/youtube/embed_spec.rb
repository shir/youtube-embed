require 'spec_helper'

describe Youtube::Embed do
  it 'has a version number' do
    expect(Youtube::Embed::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
